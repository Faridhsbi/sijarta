# from django.db import models

# # Create your models here.
# from django.db import models

# class Testimoni(models.Model):
#     rating = models.IntegerField(choices=[(i, str(i)) for i in range(1, 11)], verbose_name="Rating")
#     komentar = models.TextField(verbose_name="Komentar")

#     def __str__(self):
#         return f"Rating: {self.rating} - {self.komentar[:20]}"
